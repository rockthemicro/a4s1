1. Toate clasele au nevoie de un TypeSymbol pentru rezolvarea tipurilor
    - pun pe clasa abstracta (ASTBaseNode)
    - sau pun pe fiecare clasa in parte?

SAU
   Nu am nevoie de nimic, pentru ca in visitor ma plimb cu SymbolTable dupa mine

2. Voi folosi un ClassSymbol pentru simbolurile claselor + scope (analog MethodSymbol pt metode)



slide 429: sageata punctata inseamna "contine", iar linia continua inseamna "parinte" ca scope

(e.f()).g() <=> e1.g()


class A {

    int f() {
        return x + 1;
    }

    int x = 10;
}